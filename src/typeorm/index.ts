import { Role } from './role.entity';
import { TradeHistory } from './trade-history.entity';
import { User } from './user.entity';

const entities = [User, TradeHistory, Role];

export { User, TradeHistory, Role };
export default entities;
